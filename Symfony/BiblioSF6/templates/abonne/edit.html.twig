{% extends 'base.html.twig' %}

{% block title %}Edit Abonne{% endblock %}

{% block body %}
    <h1>Edit Abonne</h1>

    {{ include('abonne/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary mt-2" href="{{ path('app_abonne_index') }}">Retour à la liste</a>

    {{ include('abonne/_delete_form.html.twig') }}
{% endblock %}
